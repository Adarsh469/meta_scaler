# ── Python ─────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
env/
venv/
.env
.env.local

# ── Dataset — included in repo so HF Space build can copy it into image ────
# dataset/ ← intentionally NOT ignored
*.csv
# Runtime feedback logs (generated at runtime, not source)
feedback_log.jsonl

# ── Frontend — Node / Next.js ───────────────────────────────────────────────
frontend/node_modules/
frontend/.next/
frontend/out/
frontend/build/
frontend/.pnp
frontend/.pnp.*
frontend/.yarn/*
!frontend/.yarn/patches
!frontend/.yarn/plugins
!frontend/.yarn/releases
!frontend/.yarn/versions
frontend/coverage/
frontend/*.tsbuildinfo
frontend/next-env.d.ts
frontend/.vercel
frontend/.env*
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# ── OS / editor ─────────────────────────────────────────────────────────────
.DS_Store
.idea/
.vscode/
*.pem

# ── OpenEnv local artifacts ──────────────────────────────────────────────────
*.log
.openenv/
.server.pid
# ── Local dev notes ──────────────────────────────────────────────────────────
run.txt

# ── Runtime learning data (generated at runtime, not source) ─────────────────
feedback_data.jsonl
learned_heuristics.json
server.log
